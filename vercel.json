{
  "_comment": "ignoreCommand: exit 1 when lint/typecheck/test pass (Vercel deploys), exit 0 when they fail (Vercel skips deploy).",
  "ignoreCommand": "corepack enable && pnpm install && (pnpm lint && pnpm typecheck && pnpm test) && exit 1 || exit 0"
}
