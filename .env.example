# GitHub token for API rate limits (optional but recommended)
# Copy to .env.local and add your token
GITHUB_TOKEN=your_github_token_here
# Or use PAT (legacy name from serve.js)
# PAT=your_github_token_here
